---
apiVersion: v1
kind: Service
metadata:
  annotations:
      service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
  labels:
    app: phuongtd-svc
  name: phuongtd-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: udacity-final-project2
  type: LoadBalancer